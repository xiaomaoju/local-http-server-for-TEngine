services:
  tengine-server:
    build:
      context: ..
      dockerfile: server/Dockerfile
    ports:
      - "${PORT:-8082}:${PORT:-8082}"
    volumes:
      - tengine-data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  tengine-data:
